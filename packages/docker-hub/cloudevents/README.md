# 1mill/cloudevents-node-base

Experimental node base to encapsulate common dependancies across node subscribers and publishers.

## How to release

```bash
docker-compose build
docker-compose push
```
